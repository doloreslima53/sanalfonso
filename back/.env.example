# ── Aplicación ────────────────────────────────────────────
APP_ENV=development          # development | production
DEBUG=true

# ── Base de datos (PostgreSQL) ─────────────────────────────
DATABASE_URL=postgresql+asyncpg://geriatrico_user:changeme@db:5432/geriatrico

# ── CORS ───────────────────────────────────────────────────
# Separar múltiples orígenes con coma
CORS_ORIGINS=http://localhost:5173,https://sanalfonsofrontend-production.up.railway.app

# ── Panel de administración ────────────────────────────────
PANEL_PASSWORD=sanalfonso2024

# ── URL pública del backend (Railway / producción) ─────────
# Usada para construir URLs absolutas de las imágenes subidas.
# Dejar vacío en Docker/dev local (devuelve URLs relativas).
# PUBLIC_URL=https://tu-backend.railway.app
