# ── API ────────────────────────────────────────────────────
# En desarrollo el proxy de Vite maneja /api → back:8000, no hace falta.
# En producción apuntar a la URL pública del backend.
VITE_API_URL=http://localhost:8000

# ── Vite dev server ────────────────────────────────────────
# Separar múltiples hosts con coma
ALLOWED_HOSTS=sanalfonsofrontend-production.up.railway.app
